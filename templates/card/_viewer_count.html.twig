<div id="viewer-count" class="text-xs sm:text-sm font-bold text-pink-600 mb-2">
    {% if viewerCount == 1 %}
        👁 Tu es seul·e sur cette carte
    {% else %}
        👁 {{ viewerCount }} joueur·euses connecté·es
    {% endif %}
</div>
